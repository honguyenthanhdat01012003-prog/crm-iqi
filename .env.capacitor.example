# Copy thành .env.capacitor (không commit secret)
# Dùng khi: npm run build:capacitor

VITE_API_BASE_URL=https://crm-iqi.id.vn/api
VITE_SOCKET_URL=https://crm-iqi.id.vn

# iOS thường chưa dùng FCM — để false trừ khi đã cấu hình push native
VITE_NATIVE_PUSH_ENABLED=false
